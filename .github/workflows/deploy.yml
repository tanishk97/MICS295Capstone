name: Deploy to S3

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: codebuild-test2Build-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
    - uses: actions/checkout@v3
    
    - name: Deploy to S3
      run: ./deploy.sh
