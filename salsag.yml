# SalsaG Trust Pipeline Configuration
aws:
  region: us-east-1
  staging_bucket: mics295-pipeline-artifacts-bucket
  ledger_table: trust-ledger

# Skip signing - using CodeBuild IAM role
skip_signing: true

artifacts:
  compression: "gzip"
  include_sbom: true
  include_provenance: true
